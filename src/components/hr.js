import { View, Text } from 'react-native';
import React from 'react';

const hr = () => {
  return (
    <View
      style={{
        marginTop: 10,
        borderBottomColor: 'white',
        marginHorizontal: 10,
        borderBottomWidth: 1,
      }}
    />
  );
};

export default hr;
